package com.gaon.nifou.v3.ctrl.common;

import org.json.simple.JSONArray;
//import org.json.JSONObject;

import org.apache.poi.ss.usermodel.*;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;

public class download {
	
	/*
	 * String json = "JSON 데이터"; JSONArray jsonArray = new JSONArray(json);
	 * 
	 * 
	 * Workbook workbook = new XSSFWorkbook(); Sheet sheet =
	 * workbook.createSheet("JSON 데이터");
	 * 
	 * // 헤더 행 생성 Row headerRow = sheet.createRow(0);
	 * headerRow.createCell(0).setCellValue("키");
	 * headerRow.createCell(1).setCellValue("값");
	 * 
	 * // JSON 데이터를 엑셀 행으로 추가 int rowNum = 1; for (JsonNode node : jsonNode) { Row
	 * row = sheet.createRow(rowNum++);
	 * row.createCell(0).setCellValue(node.get("키").asText());
	 * row.createCell(1).setCellValue(node.get("값").asText()); }
	 * 
	 * // 엑셀 파일 생성 및 저장 FileOutputStream fileOut = new
	 * FileOutputStream("JSON_데이터.xlsx"); workbook.write(fileOut); fileOut.close();
	 * workbook.close();
	 */
}
